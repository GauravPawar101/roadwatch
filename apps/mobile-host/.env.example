# =========================================================================
# ROADWATCH MOBILE ENVIRONMENT CONFIGURATION (PUBLIC / APP BOUND)
# =========================================================================
# IMPORTANT: These variables physically bundle into the raw React Native JavaScript execution binary natively!
# DO NOT place private administrative secrets inherently inside this physical application root dynamically!

# [1] Google AI Studio Edge Model Generation Engine natively
# Safe specifically ONLY if strictly bound visually in Google Cloud Console securely locking your iOS/Android Bundle IDs.
GEMINI_API_KEY=AIzaSy...

# [2] Optional Edge Cache Structurally
# Identifies the topological Supabase cluster specifically routing generic reads gracefully
SUPABASE_URL="https://fwszrxjcyblblzzgozlf.supabase.co"

# Structurally safe purely for Public Row Level Access natively mapped inside Postgres limits flawlessly
SUPABASE_ANON_KEY="sb_publishable_55TNqEXm4snpImNUF131eQ_u8fKu-HS"

# [3] Backend Custom API Matrix execution securely
# iOS Developers: Target http://localhost:3000 explicitly
# Android Developers: Target http://10.0.2.2:3000 explicitly routing loopbacks natively!
API_GATEWAY_URL=http://localhost:3000/api/v1

# [4] Cloudflare Mass Content Network Resolution inherently
# Serves structural Edge WebP media globally mathematically completely safely natively caching images
CLOUDFLARE_R2_PUBLIC_URL=https://pub-xxxxxxxxxxxxxx.r2.dev
