apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init-sql
  namespace: roadwatch
  labels:
    app.kubernetes.io/part-of: roadwatch
    layer: platform
data:
  # Populated at deploy time by deploy-kind.ps1:
  #   kubectl create configmap postgres-init-sql \
  #     --from-file=init.sql=docker/postgres/init.sql \
  #     -n roadwatch --dry-run=client -o yaml | kubectl apply -f -
  init.sql: |
    -- Placeholder. Real schema loaded from docker/postgres/init.sql at deploy time.
    SELECT 1;
