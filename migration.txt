Migration plan: Postgres primary (Cassandra removed from documentation/tracking)

1) docker-compose.yml -> [edit]
- Ensure Postgres service is present (or active) and Cassandra service is removed for local dev.
- Update all services to depend on `postgres` readiness.
- Replace all `CASSANDRA_*` env vars with Postgres vars:
  - `DATABASE_URL=postgresql://postgres:postgres@postgres:5432/roadwatch` (or compose/env equivalents)
  - optionally `POSTGRES_HOST`, `POSTGRES_DB`, `POSTGRES_USER`, `POSTGRES_PASSWORD`.

2) docker/postgres/init.sql -> [add/confirm]
- Use this file as the canonical schema init for the `roadwatch` database.
- Run it on container startup (or as a one-time init step).

3) .env.template and per-app .env files -> [edit]
- Remove/avoid `CASSANDRA_*` variables from the primary dev flow.
- Keep only Postgres variables required by each service.
- Add a single source of truth for `DATABASE_URL` (or POSTGRES_* fields).

4) package.json edits (multiple packages) -> [edit]
- Ensure `pg` is the active DB driver again for Node services that run SQL.
- Remove `cassandra-driver` dependencies from runtime packages (leave only if some legacy script still needs it).

5) services/* DB usage -> [edit]
- Replace any remaining `cassandra-driver` client usage with `pg` pool usage.
- Replace CQL statements with SQL.
- Ensure health checks use a Postgres lightweight query (e.g., `SELECT 1`).

6) apps/gateway-api DB layer -> [major edit]
- Ensure `apps/gateway-api/src/db.ts` uses `pg` (Pool) and SQL queries.
- Remove Cassandra compatibility shim (`pool.query` over cassandra.js) if present.
- Ensure seed scripts use `pg` and Postgres DDL/INSERT/UPDATE.

7) packages/core DB adapter -> [edit]
- Ensure Postgres adapter files are present and compile.
- Remove cassandra-only code paths from core (unless purely optional/legacy).

8) scripts/tooling -> [review & edit]
- Replace any tooling that assumes CQL (`.cql`/`cqlsh`) with SQL/`psql` or pg-native code.

9) Tests & CI -> [edit]
- Update tests to use Postgres test DB connections.
- Remove Cassandra-only env vars from test harness.

10) Final cleanup -> [apply]
- Remove Cassandra-only docs references and env vars from the dev flow.
- Keep a rollback note: do not delete Cassandra runtime code until Postgres-primary works end-to-end.

