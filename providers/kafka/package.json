{
  "name": "@roadwatch/kafka",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "index.ts",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@upstash/kafka": "^1.3.5"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
